services:
  postgres:
    image: postgres:12.2-alpine
    ports:
      - "5432:5432"
    env_file:
      - ../.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../sql:/docker-entrypoint-initdb.d
    networks:
      - shared-network

  spark:
    build:
      dockerfile: infra/spark/Spark.Dockerfile
      context: ..
    ports:
      - "4041:4041"
      - "18080:18080"
      - "4040:4040"
      - "7077:7077"
    env_file:
      - ../.env
    environment:
      # Podniesiono zasoby, by obsłużyć żądanie z Airflow (4 cores, 4g mem)
      SPARK_WORKER_MEMORY: 6g
      SPARK_WORKER_CORES: 4
      SPARK_DRIVER_MEMORY: 2g
    # Zwiększono limity kontenera, aby uniknąć zabicia procesu przez OOM (Out Of Memory)
    mem_limit: 8g
    memswap_limit: 8g
    volumes:
      - ../scripts:/home/hostuser/app/apps/scripts
      - ../data:/opt/airflow/data
    networks:
      - shared-network

volumes:
  postgres_data:

networks:
  shared-network:
    driver: bridge
